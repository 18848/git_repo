#include "stdlib.h"
#include "stdio.h"
#include "conio.h"
#include "string.h"

#include "headers\readFile.h"

int main(){
    //ReadPath* lst = NULL;
    //lst = newList();

    //insert(lst, readFile());
    //showList(lst);
    readFile();

    system("pause");
    return 0;
}